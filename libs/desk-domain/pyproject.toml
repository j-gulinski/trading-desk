[project]
name = "desk-domain"
version = "0.1.0"
description = "Domain entities and market vocabulary: models, symbols, providers, quotes, curves"
requires-python = ">=3.14"
dependencies = [
    "SQLAlchemy",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
