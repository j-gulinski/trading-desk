import PagePlaceholder from '../../components/PagePlaceholder.jsx'

export default function MarketData() {
  return <PagePlaceholder note="Market Data — live tick table, LIVE/STALE flags & mini price history." />
}
